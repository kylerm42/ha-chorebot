{
  "config": {
    "step": {
      "user": {
        "title": "Set up ChoreBot",
        "description": "ChoreBot provides advanced task management with recurring tasks, streak tracking, and optional cloud sync.\n\nSelect a sync backend to enable cloud backup and cross-device synchronization, or choose None to use ChoreBot locally.",
        "data": {
          "sync_backend": "Sync Backend"
        },
        "data_description": {
          "sync_backend": "Choose TickTick for cloud sync or None for local-only operation"
        }
      }
    },
    "options": {
      "step": {
        "init": {
          "title": "Configure Points Display",
          "description": "Customize how points are displayed throughout ChoreBot. You can use custom text (e.g., 'stars', '⭐ coins') and/or an MDI icon (e.g., 'mdi:star').",
          "data": {
            "text": "Points Text",
            "icon": "Points Icon"
          },
          "data_description": {
            "text": "Display name for points (can include emojis)",
            "icon": "Optional MDI icon (e.g., mdi:star)"
          }
        }
      }
    },
    "error": {
      "invalid_auth": "Invalid authentication credentials",
      "cannot_connect": "Failed to connect to TickTick",
      "text_too_long": "Points text must be 50 characters or less",
      "icon_too_long": "Icon name must be 100 characters or less"
    },
    "abort": {
      "already_configured": "ChoreBot is already configured",
      "single_instance_allowed": "Only a single instance of ChoreBot is allowed"
    }
  },
  "selector": {
    "sync_backend": {
      "options": {
        "none": "None (Local Only)",
        "ticktick": "TickTick"
      }
    }
  }
}
